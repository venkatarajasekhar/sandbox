{"map": 
    "function(doc) { 
        if (doc.email && doc.phone) 
            emit(\"Both\", 1); 
        else if(doc.email)
            emit(\"Email\", 1);
        else if(doc.phone)
            emit(\"Phone\", 1);
        else
            emit(\"Neither\", 1);
    }"
}
