{"map": 
    "function(doc) { 
        if (doc.department) 
            emit(doc.department, doc.salary); 
    }",
"reduce": 
    "function(key, values, rereduce) {
        return sum(values);
    }"
}
